"use client";

// Hover (200ms debounce) + tap popup listing every component at a
// destination, each with Add/Remove. Implemented in Task 7.
export default function DestinationPopup() {
  return null;
}
