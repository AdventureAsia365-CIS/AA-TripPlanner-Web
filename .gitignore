# Node / Next.js
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.local
.pytest_cache/
*.egg-info/

# Build artifacts
dist/
build/
*.zip

# OS / editor
.DS_Store
Thumbs.db

# Local temp / scratch (agent + debug session artifacts — never committed)
*.out
*.tfplan.bin
/.[a-z]*.sh
/.[a-z]*.json
/.[a-z]*.txt
/.dbcheck*.py
/.probe*.sql
/package-lock.json

# AAA (AA-Booking) source docs — PRD/ERD/Figma binaries, kept locally only, not committed
docs/AAA/
